# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Auth / OIDC / JWT
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.18
httpx==0.28.1
# PyOTP for TOTP-based MFA
pyotp==2.9.0
# WebAuthn (FIDO2) — MFA hardware keys
webauthn==2.2.0

# RBAC / OPA
casbin==1.37.0
requests==2.32.3

# Rate limiting
slowapi==0.1.9

# Security headers
secure==0.3.0

# DB / ORM
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10

# PII detection (Presidio)
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.8.2

# Cryptography (AES-256-GCM)
cryptography==43.0.3

# Observability / OpenTelemetry
opentelemetry-sdk==1.29.0
opentelemetry-exporter-otlp==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0

# Secrets management client
hvac==2.3.0        # HashiCorp Vault
boto3==1.35.87     # AWS Secrets Manager fallback

# Pydantic v2 (already bundled with FastAPI >= 0.100, pin explicitly)
pydantic==2.10.3
pydantic-settings==2.7.0
